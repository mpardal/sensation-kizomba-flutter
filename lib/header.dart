import 'package:flutter/material.dart';
import 'package:my_first_app_flutter/drawer.dart';
import 'package:my_first_app_flutter/navBar.dart';

class Header extends StatelessWidget {
  const Header({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        /*appBar: navBar,
        drawer: drawer);*/
        );
  }
}
